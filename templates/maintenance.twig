{% include 'html-header.twig' %}

<body class="{{ body_class }} {{ custom_body_classes|default('') }}">
    <main class="container">
        {{ post.content|raw }}
    </main>

    {{ function('wp_footer') }}
</body>
</html>
